import 'package:flutter/material.dart';

import 'package:get/get.dart';

class ShortlistedProposalsView extends GetView {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text(
          'ShortlistedProposalsView is working',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
