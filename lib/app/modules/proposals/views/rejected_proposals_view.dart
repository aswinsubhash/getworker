import 'package:flutter/material.dart';

import 'package:get/get.dart';

class RejectedProposalsView extends GetView {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text(
          'RejectedProposalsView is working',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
