import 'package:flutter/material.dart';

import 'package:get/get.dart';

class ActiveProposalsView extends GetView {
  @override
  Widget build(BuildContext context) {
    return Scaffold(

      body: Center(
        child: Text(
          'ActiveProposalsView is working',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
